-- what are subquery? 
-- this are the queries inside queries.
use atharv;
select * from student;
select * from course;
-- select * from student, course;
select student_id from student where student_fname = 'atharv'

-- in Operators is used to check the value matches or not in the tables.
